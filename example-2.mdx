---
name: Test
title: Hi, World!
filename: example-2
---

... Continued

[//]: # (Start-of-JSX [Link])

export const Link = () => {
    return (
        <a href={[".", filename].join("/")} target={"_self"} title={title}>
            {
                name
            }
        </a>
    );
};

[//]: # (End-of-JSX [Link])