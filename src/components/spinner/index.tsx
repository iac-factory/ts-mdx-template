import React from "react";

import {Component} from "./component";

import "./index.scss";

export const Spinner = ({children}: any) => {
    return (<Component children={children}/>);
};

export default Spinner;